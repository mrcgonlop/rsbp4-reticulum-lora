# {{ ansible_managed }}
# meshtasticd top-level config. Hardware (Lora:) comes from a config.d/
# snippet symlinked from /etc/meshtasticd/available.d/ — see the role's
# tasks/main.yml. This file is for non-hardware settings only.

General:
  MaxNodes: 200
  MaxMessageQueue: 100

Webserver:
  Port: 9443
  RootPath: /usr/share/doc/meshtasticd/web

GPS:
  SerialPath: ""
